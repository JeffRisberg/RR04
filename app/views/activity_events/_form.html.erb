<%= form_for(@activity_event) do |f| %>
  <% if @activity_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@activity_event.errors.count, "error") %> prohibited this activity_event from being saved:</h2>

      <ul>
      <% @activity_event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :userActivity %><br />
    <%= f.text_field :userActivity %>
  </div>
  <div class="field">
    <%= f.label :amount %><br />
    <%= f.number_field :amount %>
  </div>
  <div class="field">
    <%= f.label :journalEntry %><br />
    <%= f.text_field :journalEntry %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
